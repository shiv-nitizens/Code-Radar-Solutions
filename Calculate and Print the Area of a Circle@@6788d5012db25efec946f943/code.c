#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int pi=3.14;
    int radius;
    float area;
    scanf("%d",&radius);
    area=pi*radius*radius
    printf("Area: %.2f",area);
}